import {useEffect,useRef,useState} from 'react'
import {BookOpen,FlaskConical,Leaf,MessageSquare,ShieldCheck,FolderOpen,ArrowUpRight,ArrowUp,Globe2,PanelLeftClose,PanelLeftOpen,Plus,Search,Settings2,Library,ClipboardCheck,Database,Check,ChevronRight,ExternalLink,Download,Mic,Square,Volume2,Bookmark,ThumbsUp,Flag,LoaderCircle,Info,LogIn,X,Scale,LockKeyhole} from 'lucide-react'
import {api,post,setCSRF,downloadJSON,streamQuestion} from './lib'
import type {Lang,Jurisdiction,Market,Citation,Answer,Turn} from './lib'
import {t} from './i18n'
import type {Key} from './i18n'
import {Button} from './components/ui/button'
import {Dialog,DialogContent,DialogTitle,DialogDescription} from './components/ui/dialog'
import {AssessmentPage,LibraryPage,CasesPage,ReviewPage,SettingsPage,AdminPage,PriorPage} from './pages'

type Page='assistant'|'classification'|'abs'|'sources'|'cases'|'reviews'|'admin'|'settings'|'prior'
const nav=[{id:'assistant',icon:MessageSquare},{id:'classification',icon:FlaskConical},{id:'abs',icon:ShieldCheck},{id:'sources',icon:Library},{id:'prior',icon:Search},{id:'cases',icon:FolderOpen}] as const

export default function App(){
 const [lang,setLang]=useState<Lang>((localStorage.getItem('ipsakti-language') as Lang)||'en')
 const [page,setPage]=useState<Page>('assistant')
 const [jurisdiction,setJurisdiction]=useState<Jurisdiction>('india')
 const [market,setMarket]=useState<Market>('treaties')
 const [user,setUser]=useState<any>(null)
 const [caps,setCaps]=useState<any>(null)
 const [coverage,setCoverage]=useState<any>(null)
 const [online,setOnline]=useState(false)
 const [auth,setAuth]=useState(false)
 const [menu,setMenu]=useState(false)
 const [error,setError]=useState('')
 const [notice,setNotice]=useState('')
 const [citation,setCitation]=useState<Citation|null>(null)
 const [histories,setHistories]=useState<Record<string,Turn[]>>({})
 const [seed,setSeed]=useState('')
 const [activeCase,setActiveCase]=useState<any>(null)
 const initial=useRef(false)
 const tr=(key:Key)=>t(key,lang)
 const key=`${jurisdiction}:${jurisdiction==='india'?'treaties':market}:${lang}`
 const turns=histories[key]||[]
 const fail=(e:unknown)=>setError(e instanceof Error?e.message:String(e))
 async function refresh(){try{const [c,v]=await Promise.all([api('/capabilities'),api('/coverage')]);setCaps(c);setCoverage(v);setOnline(true)}catch(e){fail(e);setOnline(false)}}
 async function bootstrap(){try{const s=await api('/auth/session');setCSRF(s.csrf_token);setUser(s.user);await refresh()}catch(e){fail(e);setOnline(false)}}
 useEffect(()=>{if(!initial.current){initial.current=true;void bootstrap()}},[])
 useEffect(()=>{document.documentElement.lang=lang;localStorage.setItem('ipsakti-language',lang)},[lang])
 useEffect(()=>{if(notice){const id=setTimeout(()=>setNotice(''),4500);return()=>clearTimeout(id)}},[notice])
 function viewCitation(value:Citation){setCitation(value);api<Citation>('/sources/chunks/'+value.id).then(detail=>setCitation(current=>current?.id===value.id?{...detail,support_quote:value.support_quote}:current)).catch(fail)}
 function navigate(id:Page){setPage(id);setMenu(false)}
 async function signOut(){setActiveCase(null);try{await post('/auth/logout',{});setHistories({});await bootstrap();setNotice(tr('signOut'))}catch(e){fail(e)}}
 async function saveCase(){
  if(!user){setAuth(true);return}
  if(!turns.length)return
  try{await post('/cases',{title:turns[0].question.slice(0,180),jurisdiction,market:jurisdiction==='india'?'treaties':market,confidential:true,content:{history:turns}});setNotice(tr('saved'));setPage('cases')}catch(e){fail(e)}
 }
 const shared={lang,user,onError:fail,onNotice:setNotice,onAuth:()=>setAuth(true),onCitation:viewCitation}
 return <div className="min-h-screen bg-slate-950 text-slate-100 font-sans [&_button]:cursor-pointer [&_button:disabled]:cursor-not-allowed [&_button:disabled]:opacity-50 [&_input]:max-w-full [&_input]:rounded-lg [&_input]:border [&_input]:border-slate-700 [&_input]:bg-slate-900 [&_input]:p-3 [&_input]:text-slate-100 [&_input[type=checkbox]]:accent-emerald-500 [&_select]:max-w-full [&_select]:rounded-lg [&_select]:border [&_select]:border-slate-700 [&_select]:bg-slate-900 [&_select]:p-2 [&_textarea]:rounded-lg [&_textarea]:border [&_textarea]:border-slate-700 [&_textarea]:bg-slate-900 [&_textarea]:p-3 [&_p]:leading-relaxed [&_:focus-visible]:outline-2 [&_:focus-visible]:outline-offset-2 [&_:focus-visible]:outline-emerald-400">
  <a className="sr-only focus:not-sr-only focus:fixed focus:top-2 focus:left-2 focus:z-50 focus:bg-slate-800 focus:p-3" href="#main">Skip to content</a>
  {menu&&<button className="fixed inset-0 z-20 bg-black/60 lg:hidden" aria-label={tr('close')} onClick={()=>setMenu(false)}/>}
  <aside className={`fixed inset-y-0 left-0 z-30 flex w-64 flex-col border-r border-slate-800 bg-slate-950/95 p-5 backdrop-blur-md transition-transform lg:translate-x-0 ${menu?'translate-x-0':'-translate-x-full'}`}>
   <button className="flex items-center gap-3 text-left" onClick={()=>navigate('assistant')} aria-label="IP-SAKTI Sahayak home"><span className="grid size-12 place-items-center rounded-2xl bg-emerald-950 text-emerald-400"><Leaf size={25} strokeWidth={1.6}/></span><span><strong>IP-SAKTI</strong><small>SAHAYAK</small></span></button>
   <div className="my-5 text-xs text-slate-500">{tr('tagline')}</div>
   <Button variant="outline" className="mb-7 w-full" onClick={()=>{setActiveCase(null);setHistories({...histories,[key]:[]});navigate('assistant')}}><Plus size={17}/>{tr('newChat')}</Button>
   <p className="mb-2 mt-4 text-xs uppercase tracking-widest text-slate-500">{tr('workspace')}</p>
   <nav aria-label={tr('workspace')}>{nav.map(({id,icon:Icon})=><button key={id} className={`flex items-center gap-3 my-1 w-full rounded-lg px-3 py-3 text-left text-sm text-slate-300 hover:bg-slate-800 hover:text-emerald-300 ${page===id?'bg-emerald-950/60 text-emerald-300':''}`} onClick={()=>navigate(id)}><Icon size={19}/><span>{tr(id)}</span>{page===id&&<span className="ml-auto size-1.5 rounded-full bg-emerald-400"/>}</button>)}</nav>
   {user&&<div className="mt-5 border-t border-slate-800 pt-3"><button className={`flex items-center gap-3 my-1 w-full rounded-lg px-3 py-3 text-left text-sm text-slate-300 hover:bg-slate-800 hover:text-emerald-300 ${page==='reviews'?'bg-emerald-950/60 text-emerald-300':''}`} onClick={()=>navigate('reviews')}><ClipboardCheck size={19}/>{tr('reviews')}</button>{['curator','admin'].includes(user.role)&&<button className={`flex items-center gap-3 my-1 w-full rounded-lg px-3 py-3 text-left text-sm text-slate-300 hover:bg-slate-800 hover:text-emerald-300 ${page==='admin'?'bg-emerald-950/60 text-emerald-300':''}`} onClick={()=>navigate('admin')}><Database size={19}/>{tr('admin')}</button>}</div>}
   <div className="mt-auto pt-5"><div className="flex items-center gap-3 py-4 text-xs [&_strong]:block [&_small]:block [&_small]:text-slate-500"><span className={online?'size-2 shrink-0 rounded-full bg-emerald-400':'size-2 shrink-0 rounded-full bg-emerald-400 bg-amber-400'}/><div><strong>{tr(online?'connected':'offline')}</strong><small>{coverage?.sources??'—'} {tr('sourceCount').toLowerCase()}</small></div></div><button className={`flex items-center gap-3 my-1 w-full rounded-lg px-3 py-3 text-left text-sm text-slate-300 hover:bg-slate-800 hover:text-emerald-300 ${page==='settings'?'bg-emerald-950/60 text-emerald-300':''}`} onClick={()=>navigate('settings')}><Settings2 size={18}/>{tr('settings')}</button>
   <button className="flex items-center gap-3 w-full border-t border-slate-800 pt-4 text-left text-xs [&_strong]:block [&_strong]:max-w-36 [&_strong]:truncate [&_small]:text-slate-400" onClick={()=>user?signOut():setAuth(true)}><span className="grid size-9 shrink-0 place-items-center rounded-full bg-slate-800 text-emerald-400">{user?user.email[0].toUpperCase():<LogIn size={18}/>}</span><span><strong>{user?user.email:tr('guest')}</strong><small>{user?tr('signOut'):tr('signIn')}</small></span><ChevronRight size={15}/></button></div>
  </aside>
  <div className="flex min-h-screen min-w-0 flex-col lg:ml-64">
   <header className="flex items-center gap-3 min-h-18 justify-between border-b border-slate-800 px-4 sm:px-8"><div className="flex items-center gap-3 text-xs text-slate-400"><button className="lg:hidden" aria-label="Menu" onClick={()=>setMenu(!menu)}>{menu?<PanelLeftClose size={22}/>:<PanelLeftOpen size={22}/>}</button><span>{tr('workspace')}</span><ChevronRight size={13}/><strong>{tr(page)}</strong></div><div className="flex items-center gap-3"><span className="rounded border border-emerald-800/50 bg-emerald-950/40 px-2 py-1 text-[10px] tracking-widest text-emerald-400">PILOT</span><label className="flex items-center gap-3 text-sm"><Globe2 size={16}/><select aria-label="Language" value={lang} onChange={e=>setLang(e.target.value as Lang)}><option value="en">English</option><option value="hi">हिन्दी</option><option value="mr">मराठी</option></select></label></div></header>
   <main id="main">
    {error&&<div className="flex items-center gap-3 m-4 items-start rounded-xl border border-red-800 bg-red-950/50 p-4 text-red-200 [&_button]:ml-auto" role="alert"><Info size={18}/><div><strong>{tr('error')}</strong><p>{error}</p></div><button aria-label={tr('close')} onClick={()=>setError('')}><X size={18}/></button></div>}
    {page==='assistant'&&<>
     <div className="flex items-center gap-3 flex-wrap border-b border-slate-800 px-4 py-5 sm:px-8"><div className="flex items-center gap-3 rounded-xl border border-slate-800 bg-slate-900 p-1 [&_button]:flex [&_button]:items-center [&_button]:gap-2 [&_button]:rounded-lg [&_button]:px-3 [&_button]:py-2 [&_button]:text-sm" aria-label="Jurisdiction"><button aria-pressed={jurisdiction==='india'} onClick={()=>{setActiveCase(null);setJurisdiction('india')}} className={jurisdiction==='india'?'bg-emerald-950/60 text-emerald-300':''}><span className="size-2.5 rounded-full bg-emerald-400"/>{tr('india')}</button><button aria-pressed={jurisdiction==='international'} onClick={()=>{setActiveCase(null);setJurisdiction('international')}} className={jurisdiction==='international'?'bg-emerald-950/60 text-emerald-300':''}><Globe2 size={15}/>{tr('international')}</button></div>{jurisdiction==='international'&&<select className="text-sm" aria-label="International market" value={market} onChange={e=>{setActiveCase(null);setMarket(e.target.value as Market)}}>{(['treaties','us','eu','uk'] as const).map(m=><option key={m} value={m}>{tr(m)}</option>)}</select>}<span className="ml-auto hidden items-center gap-2 text-xs text-slate-500 xl:flex"><LockKeyhole size={13}/>{tr('scopeNote')}</span></div>
     <Chat key={key} caseId={activeCase?.id} lang={lang} jurisdiction={jurisdiction} market={market} turns={turns} setTurns={value=>setHistories(h=>({...h,[key]:value}))} caps={caps} seed={seed} setSeed={setSeed} onCitation={viewCitation} onError={fail} onNotice={setNotice} onSave={saveCase} onNavigate={navigate}/>
    </>}
    {page==='classification'&&<AssessmentPage key={`classify-${lang}`} kind="classification" {...shared}/>}
    {page==='abs'&&<AssessmentPage key={`abs-${lang}`} kind="abs" {...shared}/>}
    {page==='sources'&&<LibraryPage {...shared} coverage={coverage}/>}
    {page==='cases'&&<CasesPage {...shared} onOpenCase={c=>{setActiveCase(c);setJurisdiction(c.jurisdiction);setMarket(c.market);setHistories(h=>({...h,[`${c.jurisdiction}:${c.market}:${lang}`]:c.content.history||[]}));navigate('assistant')}}/>}
    {page==='reviews'&&<ReviewPage {...shared}/>}
    {page==='prior'&&<PriorPage {...shared}/>}
    {page==='settings'&&<SettingsPage {...shared} caps={caps} onDeleted={()=>{setHistories({});void bootstrap()}}/>}
    {page==='admin'&&<AdminPage {...shared} onRefresh={refresh}/>}
   </main>
   <footer className="flex items-center gap-3 mt-auto border-t border-slate-800 px-5 py-5 text-xs text-slate-500"><Scale size={14}/><span>{tr('disclaimer')}</span><span className="ml-auto hidden whitespace-nowrap sm:block">IP-SAKTI · v1.0</span></footer>
  </div>
  <Dialog open={!!citation} onOpenChange={open=>!open&&setCitation(null)}><DialogContent sheet><DialogTitle>{tr('snapshot')}</DialogTitle><DialogDescription>{tr('original')}</DialogDescription>{citation&&<div className="mt-6 space-y-5 [&_h2]:text-2xl [&_h2]:font-semibold [&_h3]:text-lg [&_h4]:text-sm [&_blockquote]:my-3 [&_blockquote]:whitespace-pre-wrap [&_blockquote]:border-l-2 [&_blockquote]:border-emerald-700 [&_blockquote]:pl-4 [&_blockquote]:text-sm [&_blockquote]:leading-7 [&_blockquote]:text-slate-300 [&_dt]:mt-3 [&_dt]:text-xs [&_dt]:text-slate-500 [&_dd]:text-sm"><span className="mb-3 block text-xs font-semibold uppercase tracking-widest text-emerald-400">{citation.publisher}</span><h2>{citation.title}</h2><p className="flex items-center gap-3 text-sm text-emerald-400"><BookOpen size={16}/>{citation.locator}{citation.page?` · PDF ${citation.page}`:''}</p><span className={`inline-flex items-center gap-1 rounded-md border px-2 py-1 text-xs ${citation.review_status==='approved'?'border-emerald-800/50 bg-emerald-950/40 text-emerald-400':'border-amber-800/50 bg-amber-950/40 text-amber-300'}`}>{citation.review_status==='approved'?tr('approved'):tr('referenceOnly')}</span><h3>{citation.heading}</h3>{citation.support_quote&&<><h4>Verbatim supporting quote</h4><blockquote>{citation.support_quote}</blockquote></>}<details open={!citation.support_quote}><summary>Full source excerpt</summary><blockquote>{citation.excerpt}</blockquote></details><dl><dt>{tr('retrieved')}</dt><dd>{new Date(citation.retrieved_at).toLocaleDateString()}</dd><dt>{tr('reviewStatus')}</dt><dd>{citation.review_note||citation.review_status}</dd>{citation.checksum&&<><dt>{tr('checksum')}</dt><dd className="break-all font-mono text-xs">{citation.checksum}</dd></>}</dl><div className="flex items-center gap-3 mt-5 flex-wrap"><Button variant="outline" asChild><a href={citation.url} target="_blank" rel="noreferrer">{tr('official')}<ExternalLink size={15}/></a></Button>{citation.file_url&&<Button asChild><a href={citation.file_url} target="_blank" rel="noreferrer">{tr('download')}<Download size={15}/></a></Button>}</div></div>}</DialogContent></Dialog>
  <AuthDialog open={auth} onOpenChange={setAuth} lang={lang} onError={fail} onDone={value=>{setUser(value);setAuth(false);setNotice(tr('saved'))}}/>
  {notice&&<div className="fixed bottom-5 right-5 z-50 flex max-w-sm items-center gap-3 rounded-xl border border-emerald-700 bg-slate-900 p-4 text-emerald-300 shadow-xl" role="status"><Check size={18}/>{notice}</div>}
 </div>
}

function Chat({caseId,lang,jurisdiction,market,turns,setTurns,caps,seed,setSeed,onCitation,onError,onNotice,onSave,onNavigate}:{caseId?:string;lang:Lang;jurisdiction:Jurisdiction;market:Market;turns:Turn[];setTurns:(v:Turn[])=>void;caps:any;seed:string;setSeed:(v:string)=>void;onCitation:(c:Citation)=>void;onError:(e:unknown)=>void;onNotice:(v:string)=>void;onSave:()=>void;onNavigate:(p:Page)=>void}){
 const tr=(k:Key)=>t(k,lang)
 const [query,setQuery]=useState(''),[busy,setBusy]=useState(false),[cloud,setCloud]=useState(false),[confidential,setConfidential]=useState(false)
 const [stage,setStage]=useState('Searching and verifying sources')
 const requestController=useRef<AbortController|null>(null)
 useEffect(()=>()=>requestController.current?.abort(),[])
 const [recording,setRecording]=useState(false),[audio,setAudio]=useState<string|null>(null)
 const end=useRef<HTMLDivElement>(null),voice=useRef<{context:AudioContext;stream:MediaStream;processor:ScriptProcessorNode;chunks:Float32Array[];source:MediaStreamAudioSourceNode}|null>(null)
 useEffect(()=>{if(seed){setQuery(seed);setSeed('')}},[seed])
 useEffect(()=>{end.current?.scrollIntoView({behavior:'smooth',block:'nearest'})},[turns.length])
 useEffect(()=>()=>{voice.current?.stream.getTracks().forEach(t=>t.stop());void voice.current?.context.close()},[])
 async function send(value=query){if(busy||value.trim().length<3)return;setBusy(true);setQuery('');setStage('Searching and verifying sources');requestController.current=new AbortController();try{const answer=await streamQuestion({query:value,jurisdiction,market:jurisdiction==='india'?'treaties':market,language:lang,confidential:confidential||!!caseId,cloud_consent:cloud,case_id:caseId,previous_questions:turns.slice(-4).map(t=>t.question)},setStage,requestController.current.signal);setTurns([...turns,{question:value,answer}])}catch(e){if(!(e instanceof DOMException&&e.name==='AbortError')){onError(e);setQuery(value)}}finally{setBusy(false)}}
 async function feedback(answer:Answer,reason:string){try{await post('/feedback',{trace_id:answer.trace_id,rating:reason==='helpful'?5:2,reason});onNotice(tr('feedbackSaved'))}catch(e){onError(e)}}
 async function speak(answer:Answer){
  if(!window.confirm(tr('voiceConsent')))return
  try{const r=await post('/language',{task:'tts',source:answer.translation?lang:'en',text:answer.translation||answer.claims.map(c=>c.text).join('\n'),allow_cloud:true,confidential:confidential||!!caseId});setAudio('data:audio/wav;base64,'+r.audio)}catch(e){onError(e)}
 }
 async function record(){
  if(recording&&voice.current){
   const v=voice.current;v.processor.disconnect();v.source.disconnect();v.stream.getTracks().forEach(t=>t.stop());await v.context.close();voice.current=null;setRecording(false)
   const raw=new Float32Array(v.chunks.reduce((n,c)=>n+c.length,0));let offset=0;for(const chunk of v.chunks){raw.set(chunk,offset);offset+=chunk.length}
   const wav=encodeWav(raw,v.context.sampleRate);let binary='';const bytes=new Uint8Array(wav);for(let i=0;i<bytes.length;i++)binary+=String.fromCharCode(bytes[i])
   setBusy(true);try{const r=await post('/language',{task:'asr',source:lang,audio:btoa(binary),allow_cloud:true,confidential:confidential||!!caseId});setQuery(r.text)}catch(e){onError(e)}finally{setBusy(false)}return
  }
  if(confidential){onError(tr('cloudNote'));return}
  if(!caps?.bhashini.configured){onError(tr('notConfigured'));return}
  if(!window.confirm(tr('voiceConsent')))return
  try{const stream=await navigator.mediaDevices.getUserMedia({audio:true});const context=new AudioContext();const source=context.createMediaStreamSource(stream);const processor=context.createScriptProcessor(4096,1,1);const chunks:Float32Array[]=[];let samples=0;processor.onaudioprocess=e=>{if(samples<context.sampleRate*60){const data=new Float32Array(e.inputBuffer.getChannelData(0));chunks.push(data);samples+=data.length}};source.connect(processor);processor.connect(context.destination);voice.current={context,stream,source,processor,chunks};setRecording(true)}catch(e){onError(e)}
 }
 const examples=[{icon:Leaf,label:'patentLabel',query:'patentQuestion'},{icon:ShieldCheck,label:'absLabel',query:'absQuestion'},{icon:FlaskConical,label:'categoryLabel',query:'categoryQuestion'},{icon:Globe2,label:'exportLabel',query:'exportQuestion'}] as const
 return <section className={`mx-auto w-full max-w-5xl px-4 py-10 sm:px-10 ${turns.length?'pt-6':''}`}>
  {!turns.length&&<div className="mb-9 text-center [&_h1]:mx-auto [&_h1]:max-w-2xl [&_h1]:text-4xl [&_h1]:font-semibold [&_h1]:tracking-tight [&_p]:mx-auto [&_p]:mt-4 [&_p]:max-w-xl [&_p]:text-slate-400"><div className="mx-auto mb-6 grid size-16 place-items-center rounded-2xl border border-emerald-800/50 bg-emerald-950/40 text-emerald-400"><Leaf size={30} strokeWidth={1.3}/></div><div className="mb-3 block text-xs font-semibold uppercase tracking-widest text-emerald-400">{tr('evidenceFirst')}</div><h1>{tr('welcome')}</h1><p>{tr('welcomeSub')}</p></div>}
  {!!turns.length&&<div className="flex items-center gap-3 mb-6 justify-between"><span className="mb-3 block text-xs font-semibold uppercase tracking-widest text-emerald-400">{tr('assistant')} / {tr(jurisdiction)}</span><Button variant="outline" size="sm" onClick={onSave}><Bookmark size={15}/>{tr('save')}</Button></div>}
  <div className="space-y-8">{turns.map((turn,index)=><article key={turn.answer.trace_id} className="min-w-0"><div className="flex items-start gap-4 border-b border-slate-800 pb-5 [&_h2]:text-xl [&_h2]:font-medium"><span className="pt-1 font-mono text-xs text-slate-500">{String(index+1).padStart(2,'0')}</span><h2>{turn.question}</h2></div><div className="py-7"><div className="flex items-center gap-3 mb-6 flex-wrap [&_small]:ml-auto [&_small]:text-xs [&_small]:text-slate-500"><span className="grid size-9 shrink-0 place-items-center rounded-xl bg-emerald-950/60 text-emerald-400"><Leaf size={17}/></span><strong>Sahayak</strong><span className={`inline-flex items-center gap-1 rounded-md border px-2 py-1 text-xs ${turn.answer.evidence_status==='supported'?'border-emerald-800/50 bg-emerald-950/40 text-emerald-400':turn.answer.evidence_status==='partial'?'border-amber-800/50 bg-amber-950/40 text-amber-300':'border-slate-700 bg-slate-800/50 text-slate-400'}`}>{tr(turn.answer.evidence_status as Key)}</span><small>{tr(turn.answer.mode as Key)}</small></div><h3>{turn.answer.heading}</h3>{turn.answer.translation&&<div className="rounded-2xl border border-slate-800 bg-slate-900/60 backdrop-blur-md mb-5 p-5"><span className="mb-3 block text-xs font-semibold uppercase tracking-widest text-emerald-400">{tr('translation')}</span><p>{turn.answer.translation}</p></div>}{turn.answer.mode==='retrieval'&&<p className="flex items-center gap-3 items-start rounded-xl border border-teal-800/50 bg-teal-950/30 p-4 text-sm text-teal-200 [&_svg]:shrink-0">{tr('excerptNotice')}</p>}<div className="mt-5 space-y-5 text-sm leading-8 text-slate-300 [&_p]:whitespace-pre-wrap [&_p]:break-words">{turn.answer.claims.map((claim,i)=><p key={i} className={turn.answer.mode==='retrieval'?'border-l-2 border-emerald-800 pl-4':undefined}>{claim.kind==='clarification'&&<strong>{tr('clarificationLabel')} </strong>}{claim.text.replace(/\s+/g,' ').trim()} {claim.citation_ids.map(id=><button className="mx-1 inline-grid min-w-6 place-items-center rounded border border-emerald-800/50 bg-emerald-950/40 px-1 text-xs text-emerald-400 hover:bg-emerald-900" key={id} onClick={()=>{const c=turn.answer.citations.find(c=>c.id===id);if(c)onCitation({...c,support_quote:claim.support_quote})}} aria-label={tr('viewSource')}>{turn.answer.citations.findIndex(c=>c.id===id)+1}</button>)}</p>)}</div>{!!turn.answer.citations.length&&<div className="my-6"><span className="mb-3 block text-xs font-semibold uppercase tracking-widest text-emerald-400">{tr('citations')}</span><div className="grid gap-2 [&>button]:flex [&>button]:items-center [&>button]:gap-3 [&>button]:rounded-xl [&>button]:border [&>button]:border-slate-800 [&>button]:bg-slate-900/60 [&>button]:p-4 [&>button]:text-left [&>button:hover]:border-emerald-800 [&_strong]:block [&_strong]:text-sm [&_small]:block [&_small]:text-xs [&_small]:text-slate-400 [&>button>span]:text-emerald-400 [&>button>div]:min-w-0 [&>button>div]:flex-1 [&_svg]:shrink-0 [&_svg]:text-emerald-400">{turn.answer.citations.map((c,i)=><button key={c.id} onClick={()=>onCitation({...c,support_quote:turn.answer.claims.filter(claim=>claim.citation_ids.includes(c.id)).map(claim=>claim.support_quote).filter(Boolean).join('\n\n')})}><span>{i+1}</span><div><strong>{c.title}</strong><small>{c.locator.slice(0,85)}</small></div><ArrowUpRight size={16}/></button>)}</div></div>}<details className="mt-5 border-t border-slate-800 pt-4 text-sm text-slate-400 [&_summary]:flex [&_summary]:items-center [&_summary]:gap-2 [&_summary]:cursor-pointer [&_ul]:mt-3 [&_ul]:list-disc [&_ul]:pl-5" open={true}><summary><Info size={15}/>{tr('limitations')}</summary><ul>{turn.answer.limitations.filter(l=>typeof l==='string'&&l.trim()).map((l,i)=><li key={i}>{l}</li>)}</ul></details><div className="mt-5 flex flex-wrap items-center gap-4 text-xs text-slate-500 [&_button]:flex [&_button]:items-center [&_button]:gap-2 [&_button:hover]:text-emerald-300 [&_small]:ml-auto"><button onClick={()=>feedback(turn.answer,'helpful')}><ThumbsUp size={15}/>{tr('helpful')}</button><button onClick={()=>feedback(turn.answer,'citation')}><Flag size={15}/>{tr('reportIssue')}</button><button onClick={()=>downloadJSON('ipsakti-answer.json',turn.answer)}><Download size={15}/>{tr('export')}</button><button onClick={()=>speak(turn.answer)} disabled={!caps?.bhashini.configured}><Volume2 size={15}/>{tr('listen')}</button><small>{turn.answer.metrics.retrieval} · {Math.round((turn.answer.metrics.elapsed_ms||0)/100)/10}s</small></div></div></article>)}<div ref={end}/></div>
  {busy&&<div className="flex items-center gap-3 py-5 text-sm text-emerald-400" role="status"><LoaderCircle size={18} className="animate-spin"/>{stage}</div>}
  <form className="rounded-2xl border border-slate-800 bg-slate-900/60 backdrop-blur-md overflow-hidden focus-within:ring-2 focus-within:ring-emerald-500/40 [&_textarea]:min-h-28 [&_textarea]:w-full [&_textarea]:resize-none [&_textarea]:border-0 [&_textarea]:bg-transparent [&_textarea]:p-5" onSubmit={e=>{e.preventDefault();void send()}}><textarea aria-label={tr('ask')} placeholder={tr('ask')} value={query} onChange={e=>setQuery(e.target.value)} maxLength={4000} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey&&!e.nativeEvent.isComposing){e.preventDefault();void send()}}}/><div className="flex flex-wrap items-center justify-between gap-3 px-4 pb-4 [&>span]:flex [&>span]:items-center [&>span]:gap-2 [&>span]:text-xs [&>span]:text-slate-500 [&>div]:flex [&>div]:items-center [&>div]:gap-3"><span><BookOpen size={14}/>{caps?.generation.configured ? caps.generation.model : tr('retrievalOnly')} · {caps?.embeddings.enabled ? tr('semanticSearch') : tr('keywordSearch')}</span><div><button type="button" className={`rounded-lg p-2 text-slate-400 hover:bg-slate-800 ${recording?'text-red-400':''}`} title={tr(recording?'stop':'voice')} aria-label={tr(recording?'stop':'voice')} onClick={record}>{recording?<Square size={18}/>:<Mic size={19}/>}</button><Button disabled={busy||query.trim().length<3} type="submit">{tr('send')}<ArrowUp size={17}/></Button></div></div></form>
  <div className="my-4 flex flex-wrap justify-center gap-5 text-xs text-slate-400 [&_label]:flex [&_label]:items-center [&_label]:gap-2"><label><input type="checkbox" checked={confidential} onChange={e=>{setConfidential(e.target.checked);if(e.target.checked)setCloud(false)}}/><LockKeyhole size={13}/>{tr('confidential')}</label><label><input type="checkbox" checked={cloud} disabled={confidential} onChange={e=>setCloud(e.target.checked)}/>{tr('cloud')}</label></div>
  {cloud&&<p className="text-center text-xs text-amber-300">{tr('cloudNote')}</p>}
  {audio&&<audio controls autoPlay src={audio} className="my-4 w-full"/>}
  {!turns.length&&<div className="mt-9"><div className="mb-3 block text-xs font-semibold uppercase tracking-widest text-emerald-400">{tr('suggested')}</div><div className="grid gap-3 sm:grid-cols-2 [&_button]:rounded-xl [&_button]:border [&_button]:border-slate-800 [&_button]:bg-slate-900/60 [&_button]:p-5 [&_button]:text-left [&_button]:backdrop-blur-md [&_button:hover]:border-emerald-800 [&_button>div]:mb-4 [&_button>div]:flex [&_button>div]:justify-between [&_svg]:text-emerald-400 [&_p]:mt-2 [&_p]:text-sm [&_p]:text-slate-400">{examples.map(({icon:Icon,label,query:q})=><button key={q} onClick={()=>{if(q==='exportQuestion'&&jurisdiction==='india'){onNotice(tr('scopeNote'));setQuery(tr(q))}else setQuery(tr(q))}}><div><Icon size={20}/><ArrowUpRight size={16}/></div><strong>{tr(label)}</strong><p>{tr(q)}</p></button>)}</div><div className="flex items-center gap-3 mt-6 flex-wrap border-t border-slate-800 pt-5 text-xs text-slate-400 [&_button]:flex [&_button]:items-center [&_button]:text-emerald-400"><FlaskConical size={17}/><span>{tr('classificationSub')}</span><button onClick={()=>onNavigate('classification')}>{tr('classification')}<ChevronRight size={15}/></button></div></div>}
 </section>
}

function AuthDialog({open,onOpenChange,lang,onError,onDone}:{open:boolean;onOpenChange:(v:boolean)=>void;lang:Lang;onError:(e:unknown)=>void;onDone:(user:any)=>void}){
 const [mode,setMode]=useState<'login'|'register'>('login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false)
 const tr=(k:Key)=>t(k,lang)
 async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);try{const result=await post('/auth/'+mode,{email,password});setCSRF(result.csrf_token);setPassword('');onDone(result.user)}catch(e){onError(e)}finally{setBusy(false)}}
 return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent><span className="grid size-9 shrink-0 place-items-center rounded-xl bg-emerald-950/60 text-emerald-400"><Leaf size={23}/></span><DialogTitle>{tr(mode==='login'?'signIn':'register')}</DialogTitle><DialogDescription>{tr('authSub')}</DialogDescription><form onSubmit={submit} className="mt-6 grid gap-5 [&_label]:grid [&_label]:gap-2 [&_label]:text-sm"><label>{tr('email')}<input autoComplete="email" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>{tr('password')}<input autoComplete={mode==='login'?'current-password':'new-password'} type="password" minLength={12} maxLength={128} required value={password} onChange={e=>setPassword(e.target.value)}/></label><Button disabled={busy}>{busy?<LoaderCircle className="animate-spin" size={18}/>:tr(mode==='login'?'signIn':'register')}</Button><Button type="button" variant="ghost" onClick={()=>setMode(mode==='login'?'register':'login')}>{tr(mode==='login'?'register':'signIn')}<ArrowUpRight size={15}/></Button></form></DialogContent></Dialog>
}

function encodeWav(input:Float32Array,sampleRate:number){
 const rate=16000,ratio=sampleRate/rate,length=Math.min(Math.floor(input.length/ratio),rate*60)
 const buffer=new ArrayBuffer(44+length*2),view=new DataView(buffer)
 const str=(offset:number,value:string)=>{for(let i=0;i<value.length;i++)view.setUint8(offset+i,value.charCodeAt(i))}
 str(0,'RIFF');view.setUint32(4,36+length*2,true);str(8,'WAVE');str(12,'fmt ');view.setUint32(16,16,true);view.setUint16(20,1,true);view.setUint16(22,1,true);view.setUint32(24,rate,true);view.setUint32(28,rate*2,true);view.setUint16(32,2,true);view.setUint16(34,16,true);str(36,'data');view.setUint32(40,length*2,true)
 for(let i=0;i<length;i++){const s=Math.max(-1,Math.min(1,input[Math.floor(i*ratio)]));view.setInt16(44+i*2,s<0?s*32768:s*32767,true)}return buffer
}
